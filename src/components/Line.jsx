const Line = ({mt="mt-5", mb="mb-0"}) => {
  return (
    <div className={`flex flex-col divide-y divide-gray-200 ${mt} ${mb}`}>
      <div></div>
      <div></div>
    </div>
  );
};

export default Line;